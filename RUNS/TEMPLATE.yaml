# TEMPLATE.yaml — wzorzec dla nowych projektów
# Każdy nowy projekt stworzony przez "+" kopiuje stąd strukturę i zeruje flow.
# Modele, defaults i linux_backend są konfigurowane globalnie w app_config.yaml.
#
project_folder: ""

use_test_flow: false

# ──────────────────────────────────────────────────────────
# FLOW — lista generacji
# ──────────────────────────────────────────────────────────
flow: []
